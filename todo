-- see your own username
-- getting a message during writing terminates message
-- make a groupchat part 
-- two seperate streams handeling input and output (two threads)
    - message gets overwritten